syntax = "proto3";
package signature;

option go_package = "./signature";

// Message object has the payload and the sender of the message.
message Message {
    string sender = 1;
    bytes payload = 2;
}